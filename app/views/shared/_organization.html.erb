<div class="card-organization" data-controller="click-button" data-action="click->click-button#highlightOrg">
  <%= cl_image_tag organization.photo.key %>
  <h2><%=organization.name %></h2>
  <p><%= organization.description %></p>
  </div>
